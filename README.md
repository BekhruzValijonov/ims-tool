# IMS Tool

Настольное приложение для учёта измерительных приборов на заводе: реестр,
журнал движений и отчёты. Tauri 2 + React 19, база — один файл SQLite на
машине. Сервера у приложения нет.

Приложение отвечает на четыре вопроса, ради которых оно и существует:

1. Где сейчас находится прибор с инвентарным номером N?
2. Что сейчас на руках у сотрудника Иванова?
3. Что происходило с этим прибором за последний год?
4. У каких приборов истекает поверка?

Ни на один из них таблица «текущее состояние» без истории ответить не может,
поэтому журнал операций — центральная сущность, а не побочный лог.

Полный дизайн — [docs/superpowers/specs/2026-09-18-ims-tool-design.md](docs/superpowers/specs/2026-09-18-ims-tool-design.md).

## Принципы

- **Локально.** Данные лежат файлом SQLite в каталоге приложения. Сети,
  сервера и учётных записей нет.
- **Статус меняется только операцией.** Ни форма правки, ни репозиторий не
  дают выставить его напрямую — нельзя «просто поменять статус» и оставить
  прибор числящимся за уволившимся сотрудником.
- **У каждой записи есть автор.** Учётных записей нет, есть подпись: оператор
  один раз вводит ФИО, и оно попадает в каждую запись журнала. Смена имени не
  переписывает прошлое.
- **Ничего не удаляется.** Прибор списывается, справочник архивируется. Журнал
  обязан пережить прибор, иначе система теряет то, ради чего заведена.
- **Выгрузка — книга Excel, написанная вручную** (`src/shared/xlsx.ts` и
  `zip.ts`). CSV Excel открывает, но каждый раз с оговорками: разделитель
  зависит от локали, кодировку приходится угадывать, ширина колонок теряется.
  Книга открывается двойным щелчком, с закреплённой строкой заголовков, и
  числа в ней остаются числами. Библиотеки для этого нет: книга — это zip с
  шестью небольшими файлами XML, и весь нужный формат уместился в два модуля,
  тогда как готовый пакет притащил бы в сборку разбор формул, картинок и
  сводных таблиц. Проверяется он не «что-то вернулось», а разбором готового
  архива: контрольные суммы, центральный каталог, содержимое листа.
- **У отчёта период необязателен.** Пустые поля значат «без ограничения»:
  подставленный «последний месяц» прятал бы почти весь реестр, ничего об этом
  не говоря. Даты подписаны тем событием, которое ограничивают, — у ведомости
  это дата заведения, у списка на руках дата выдачи, — и итог под таблицей
  всегда повторяет период словами.
- **Где данные лежат — забота приложения.** Экран настроек говорит одно:
  внесённое хранится на этом компьютере и никуда не денется. Кладовщику
  незачем знать про SQLite, про каталог приложения и про то, чем отличается
  браузерный режим; ему важно, что завтра всё будет на месте.
- **Числа на экране не расходятся.** Спарклайны плиток считаются по тому же
  журналу, что и сами плитки; «всего» в бублике и «всего» на плитке — одно и
  то же число.
- **Нет мёртвых кнопок.** Недоступная операция не гасится, а отсутствует.

## Запуск

```bash
yarn install
yarn dev            # вёрстка в браузере: http://localhost:1420
yarn tauri dev      # приложение целиком, нужен toolchain Rust
yarn test           # 187 тестов
yarn verify         # типы и тесты одной командой — перед пушем
yarn build          # релизная сборка фронтенда
```

В браузере данными заведует `MemoryRepo`, и при первом открытии сеется витрина:
завод на 48 приборов с историей за полтора месяца — иначе дашборд, графики и
отчёты не на чем смотреть. Снимок её содержимого лежит в localStorage
(`src/data/browserStore.ts`), поэтому заведённый прибор и выбранное оформление
переживают перезагрузку страницы: без этого проверять что-либо в браузере
приходилось бы с нуля после каждого F5. `?demo=empty` оставляет приложение
пустым и забывает сохранённое: так проверяются первый запуск и окно
«Представьтесь».

## Как устроено

```
src/
  app/             тема, роутер, контекст, разметка
  data/            AppRepo, SqliteRepo, MemoryRepo, миграции, контракт
  features/
    instruments/   {domain,ui}
    operations/    переходы состояний, журнал, окна операций
    directories/   справочники
    verification/  поверки
    dashboard/     плитки и графики
    reports/       отчёты
    branding/      оформление: акцент, шрифт, серая шкала, скругление
  pages/           экраны
  platform/        окружение, сохранение файлов
  shared/          даты, CSV, результат, страницы
  tour/            обход «Как это работает» и шаги по экранам
```

**Слой данных — порт с двумя реализациями.** `SqliteRepo` работает на
устройстве через `@tauri-apps/plugin-sql`, `MemoryRepo` — в браузере и тестах.
Требования к ним одни и те же и проверяются одним набором `repoContract.ts`:
разные тесты для разных реализаций означали бы, что реализации разъедутся, а
ошибка вылезет на устройстве, где её дороже всего ловить. В тестах драйвером
`SqliteRepo` служит встроенный в Node модуль `node:sqlite`, поэтому по
настоящему движку SQLite гоняется тот же SQL, который пойдёт на устройстве.

**Миграции** — нумерованный массив в `src/data/migrations.ts`, применённая
версия в `PRAGMA user_version`. Миграция, попавшая в сборку, больше никогда не
редактируется: только добавляется новая в конец.

**Переходы состояний** — чистая функция `applyOperation`: ни базы, ни часов
внутри, ни React. Репозиторий только сохраняет её результат одной транзакцией.

## Что нужно знать при доработке

- **Поиск по кириллице идёт по отдельной колонке `search_text`.** Функция
  `lower()` в SQLite знает только латиницу: «Мультиметр» она оставляет как
  есть. Текст приводится к нижнему регистру в приложении и хранится готовым —
  при изменении названия колонку надо пересчитать.
- **У плагина `sql` нет своего API транзакций,** а запросы идут через пул sqlx.
  Все изменяющие операции выстроены в очередь на стороне JS и обёрнуты в
  `BEGIN IMMEDIATE`: пул отдаёт свободное соединение и создаёт новое, только
  когда свободных нет, так что одна запись за раз снимает вопрос.
- **Дизайн-система перенесена из проекта `dashboard-ui`** (шаблон Minimal), но
  без MUI: там она выражена темой MUI, здесь те же значения — палитра,
  типографика, тени, скругления — объявлены переменными CSS в
  `src/ui/tokens.css`. Числа совпадают до последней цифры.
- **Компоненты свои** (`src/ui/`): кнопка, карточка, поля, список, таблица,
  модальное окно, метка, сообщение. Библиотеки интерфейса в зависимостях нет —
  кроме календаря: там своя реализация означала бы високосные годы, недели с
  понедельника, названия месяцев в родительном падеже и управление стрелками,
  и всё это заново. Диалог сделан на нативном `<dialog>` — он сам переносит
  фокус внутрь, запирает его там и закрывается по Esc; список на обычном
  `<select>` — он работает с клавиатуры и знает про экранные читалки.
- **Таблица своя** (`src/ui/DataTable.tsx`). От прежнего DataGrid нужны были
  шапка, строки, пустое состояние и постраничность, а приезжали сортировка,
  фильтры, виртуализация и локализация, которыми экраны не пользуются.
  Раскладка закреплена (`table-layout: fixed`), длинные названия обрезаются
  многоточием: иначе строки разной высоты, и список нечем читать.
- **Знак приложения набран полосами** (`src/ui/logoShapes.ts`). Приём
  инженерных марок: буквы режутся восемью горизонтальными полосами, и знак
  остаётся собой в одном цвете — на бланке, на оттиске, в иконке 32 пикселя.
  Буквы свои, геометрические, а не вырезанные из чужого шрифта. «S» скруглена
  на плечах не для красоты: прямоугольная «эска» в точности повторяет очертания
  цифры 5, и знак читается как «IM5». Начертание лежит в одном модуле, из
  которого рисуется и разметка, и иконки приложения, — второй экземпляр
  координат разъехался бы с первым при первой же правке.
- **Иконки свои** (`src/ui/icons.tsx`), а не Iconify: тот подгружает наборы по
  сети, а приложение работает на заводском ПК, где интернета может не быть.
- **Графики на ApexCharts** — той же библиотеке, что в `dashboard-ui`. Общие
  настройки собраны один раз в `src/ui/Chart.tsx`: тонкие оси, пунктирная
  сетка, градиентная заливка под линией. На светлой схеме крупные фигуры
  заливаются не в полную силу (`fillDensity`): цвета состояний подобраны так,
  чтобы точка читалась на белом, и сектор бублика той же плотности выходит
  тяжёлым — краски на нём в сотни раз больше. Обводки у столбцов нет: она
  рисуется цветом столбца и читается как рамка, от которой он кажется гуще.
- **Оформление настраивается, язык учёта — нет** (`src/features/branding/`).
  На экране «Брендирование» задаются акцент, шрифт, серая шкала, скругление и
  плотность таблиц; цвета состояний прибора среди них нет намеренно — это
  язык, одинаковый на всех рабочих местах, а не вкус. Выбранный акцент
  доводится расчётом: тон под курсором, цвет подписи и светлый вариант для
  тёмной схемы считаются по контрасту, поэтому подпись на кнопке читается при
  любом выборе, а сама кнопка не сливается со страницей. Значения уходят
  переменными CSS в отдельный тег `<style>`: у тёмной схемы свой акцент, и
  вторым селектором его в `style="…"` не записать. Настройка лежит там же, где
  данные, — это оформление учётной системы предприятия, а не предпочтение
  браузера.
- **Шрифты интерфейса выбраны по кириллице.** У DM Sans и Barlow из
  дизайн-системы её нет вовсе: русский текст доставался подставному системному
  шрифту, то есть типографика приложения на его же языке не работала. Остались
  IBM Plex Sans (пара к моноширинному Plex, которым набраны данные), Inter,
  Golos Text и системный. Объявлены все, качается только выбранный — браузер
  берёт файл шрифта, лишь когда им что-то набрано.
- **Цвет означает состояние прибора — и больше ничего.** Палитра взята у
  приборной шкалы: у манометра белая эмаль, чёрные деления и два цветных
  сектора, рабочий и недопустимый. Здесь так же. Зелёный — прибор на месте,
  чертёжный синий — за ним стоит человек, латунь — вне строя, красный —
  требует действия сегодня. Ремонт и поверка помечены одинаково намеренно:
  цвет несёт тяжесть состояния, причину несёт подпись рядом. Те же цвета идут
  в графики — язык один на весь интерфейс.
- **Интерфейс цвета не носит.** Кнопки, пункты меню, флажки и рамка фокуса
  набраны графитом. Пока фирменный цвет работал и за «нажимаемое», и за
  «прибор в наличии», его было столько, что он перестал что-либо значить.
  Теперь любое цветное пятно на экране — это состояние прибора, и цвет снова
  читается. Единственное исключение — ссылка в данных: инвентарный номер, по
  которому открывают прибор, набранный цветом текста тонул среди соседних
  столбцов. У неё свой токен `--link`, а не акцент: акцент по умолчанию
  графитовый и меняется в «Брендировании», а ссылка не должна пропадать от
  чужого выбора.
- **Помечается исключение, а не норма.** Архивная запись справочника получает
  метку, работающая — нет. Состояние прибора в списке — точка и слово, а не
  залитая метка: в списке из двадцати пяти строк заливка складывается в
  цветной столбец, который заметнее самих данных. Полоса «требует действия»
  тоже помечена мягкой заливкой, а не залита красным насквозь: тревогу держат
  слово и крупные числа, цвет её только помечает.
- **Моноширинный шрифт только для данных:** инвентарных номеров, серийников,
  дат и чисел в колонках. Цифры одной ширины выстраиваются друг под другом, и
  столбец читается взглядом, а не посимвольно.
- **Обе схемы равноправны,** светлая по умолчанию. Схема живёт атрибутом
  `data-theme` на `<html>`, переменные CSS переключаются сами, выбор
  запоминается. Цвета состояний для схем разные (`useStateColors`): на тёмном
  фоне берутся светлые тона палитры, иначе насыщенный красный перестаёт быть
  сигналом. Хук, а не переменные: цвет уходит в настройки графиков, где
  `var()` не раскрывается.
- **Прокручивается документ, а не область содержимого** — как в дизайн-системе:
  полоса прокрутки идёт у края окна. Боковая панель и шапка закреплены, иначе
  на длинном списке меню уезжало бы вверх. Исключение — экраны со списком: они
  занимают окно ровно и отдают прокрутку таблице, чтобы её шапка и фильтры
  оставались на виду. Место под полосу зарезервировано у `html` постоянно,
  поэтому переход с экрана, который помещается целиком, на длинный ничего не
  сдвигает.
- **Пока открыто модальное окно, документ под ним стоит на месте**
  (`src/ui/scrollLock.ts`). Нативный `<dialog>` запирает фокус и нажатия, но не
  прокрутку: без запрета человек крутит колесо, чтобы прочитать что-то в окне, а
  уезжает то, к чему окно относится. Запрет ставится не `overflow: hidden` —
  непрокручиваемый документ теряет положение, и окно, открытое из середины
  длинной страницы, отматывало бы её в начало; тело закрепляется и сдвигается
  вверх ровно на прокрученное, так что картинка не меняется вовсе.
- **Содержимое стоит колонкой по центру** — как контейнер дизайн-системы
  (`Container maxWidth="xl"`, 1536 пикселей). Содержимое шапки выровнено по той
  же колонке, поэтому поиск стоит ровно над заголовком экрана.
- **Шапка лежит поверх содержимого, а не отнимает у него полосу сверху.**
  Отступ сверху отдан странице (`src/ui/Page.module.css`), поэтому прокрутка
  начинается под шапкой и содержимое уезжает ей за спину. Подложка с размытием
  и мягкая тень проявляются, только когда страница действительно прокручена: на
  ровном фоне размывать нечего, и заливка читалась бы сплошной полосой. На
  экранах со списком прокрутку забирает таблица, она стоит ниже шапки, и та
  остаётся прозрачной.
- **Карточка «Где приборы сейчас» — аккордеон** (`src/ui/Accordion.tsx`).
  Развёрнутое дерево «подразделение → места» в карточку не помещалось, и
  внутренняя полоса прокрутки лезла на числа у правого края. Свёрнутый список
  отвечает на вопрос «в каком цехе искать», раскрытый — «в каком шкафу», и
  прокручивать внутри карточки больше нечего.
- **У каждого экрана есть обход «Как это работает»** (`src/tour/`). Шаг
  привязан к элементу атрибутом `data-tour`, а не селектором по классу: класс
  переживает не всякую правку вёрстки и уносит подсказку молча. Шаг, которому
  нечего показать, пропускается — обход не подсвечивает пустоту. Про driver.js
  знает один модуль `startTour.ts`, и он же слушает прокрутку на фазе
  погружения: приложение прокручивается внутри своих контейнеров, а driver.js
  следит только за окном. Подгружается всё это по нажатию — открывают обход
  изредка, а весит он больше любого экрана. Тест сверяет якоря шагов с
  разметкой, поэтому переименованный `data-tour` ломает сборку, а не подсказку.
- **Даты вводятся через `DateInput`,** а не нативным `<input type="date">`:
  нативное поле показывает дату в формате локали системы, и на английской
  Windows кладовщик увидел бы mm/dd/yyyy. Формат задан приложением —
  ДД.ММ.ГГГГ везде. Дату можно и набрать, и выбрать: рядом с полем кнопка
  календаря (`react-day-picker`, русские подписи, неделя с понедельника).
  Набор оставлен, потому что известное число впечатывают быстрее, чем ищут
  мышью; календарь нужен, когда считают «какая пятница была на той неделе».
  Сам календарь подгружается по нажатию и лежит в потоке страницы с
  `position: fixed` — портал в `body` ушёл бы под затемнение нативного
  `<dialog>`, а прокручиваемый предок обрезал бы панель.

## Чего пока нет

Ролей и входа · импорта из Excel (схема готова: уникальный инвентарный номер,
источник записи и внешний код строки) · сканера штрихкодов · фотографий
приборов · уведомлений о сроках · печати свидетельств · бронирования · работы
по сети.
