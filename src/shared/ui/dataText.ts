import { MONO, TABULAR } from "../../app/theme/tokens"

/**
 * Начертание для данных: инвентарных номеров, серийников, дат и чисел.
 *
 * Не для подписей и заголовков — там моноширинный только создаёт шум. Смысл в
 * колонках: цифры одной ширины выстраиваются друг под другом, и столбец
 * читается взглядом, а не посимвольно.
 */
export const monoSx = { fontFamily: MONO, ...TABULAR } as const
