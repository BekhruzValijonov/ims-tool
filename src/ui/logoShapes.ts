/**
 * Начертание знака IMS.
 *
 * Буквы сложены из прямоугольников и двух диагоналей, а поперёк них идут восемь
 * полос — приём, которым набирают инженерные марки со времён Пола Рэнда: знак
 * остаётся собой и в оттиске на корпусе прибора, и в одном цвете на бланке.
 * Буквы свои, геометрические, а не наборные из чужого шрифта.
 *
 * Размеры живут здесь, а не в разметке: тем же начертанием рисуется иконка
 * приложения, и второй экземпляр координат разъехался бы с первым при первой
 * же правке.
 */

/** Высота прописной. Восемь полос по 8 с зазором 2 укладываются в неё ровно. */
export const CAP = 78

/** Общая ширина знака: I(48) + 16 + M(76) + 16 + S(64). */
export const WIDTH = 220

export const BAR = 8
export const GAP = 2
export const BARS = 8

/** Прямоугольники букв. Диагонали «М» вынесены отдельно — они не прямоугольные. */
export const RECTS: readonly { x: number; y: number; w: number; h: number }[] = [
  // I — стойка с плитами сверху и снизу
  { x: 0, y: 0, w: 48, h: 16 },
  { x: 16, y: 0, w: 16, h: CAP },
  { x: 0, y: 62, w: 48, h: 16 },

]

/** M: внешний контур с провалом посередине. Начинается от левого нижнего угла. */
export const M_PATH = "M64 78V0h20l18 35L120 0h20v78h-16V27l-18 33h-8L80 27v51z"

/**
 * S: осевая линия, обведённая толщиной буквы.
 *
 * Плечи скруглены не для красоты. Прямоугольная «эска» из трёх перекладин и
 * двух стоек в точности повторяет очертания цифры 5, и знак читается как «IM5»;
 * поворот штриха на плечах — единственное, чем эти два знака различаются.
 */
export const S_PATH = "M212 8H182c-14 0-18 8-18 15.5S168 39 182 39h12c14 0 18 8 18 15.5S208 70 194 70h-30"
export const S_STROKE = 16

/** Полосы поперёк знака: белые — то, что видно, зазоры между ними съедают букву. */
export function bars(): readonly { y: number; h: number }[] {
  return Array.from({ length: BARS }, (_, index) => ({ y: index * (BAR + GAP), h: BAR }))
}
